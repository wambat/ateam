%h1 Listing mutants

%table.ui.table
  %tr
    %th Name
    %th Teams
    %th
    %th
    %th

  - @mutants.each do |mutant|
    %tr
      %td= mutant.name
      %td
        - mutant.teams.each do |team|
          = link_to team.name, edit_admin_team_path(team)
          %br/
      %td= link_to 'Edit', edit_admin_mutant_path(mutant), {class: "ui small button"}
      %td= link_to 'Destroy', admin_mutant_path(mutant), :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "ui small button red" if mutant.destroyable?

%br

= link_to 'New Mutant', new_admin_mutant_path, class: "ui small button blue"
